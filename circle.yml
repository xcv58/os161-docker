machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t xcv58/os161 .

test:
  override:
    - docker run -ti --name os161 xcv58/os161
    - cd os161/root
    - sys161 kernel
    - q
    - exit
